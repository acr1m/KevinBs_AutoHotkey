;; Here's where the real User Autorun.ahk script is loocated..
;;
;; You have to used this one because it's the targeted location
;; by the Global Autorun.ahk script.
;; 
;; E:\Library\OneDrive\Documents\AutoHotkey\SciTE\Autorun.ahk

; SciTE4AutoHotkey v3 user autorun script
;
; You are encouraged to edit this script!
;

;; #NoEnv
;; #NoTrayIcon
;; SetWorkingDir, %A_ScriptDir%

;; grabs the object-instance of SciTE

;; o_SciTE := GetSciTEInstance()
;; if !o_SciTE
;; {
	;; MsgBox, 16, SciTE4AutoHotkey, Cannot find SciTE!
	;; ExitApp
;; }
